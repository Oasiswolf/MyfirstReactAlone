import React, { Component } from 'react'

import InnerColor from './innerColor'

export default class Game extends Component {
    render() {
        return(
            <div>
                <h1>Main Color to Match</h1>
            </div>
        )
    }
}